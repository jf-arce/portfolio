---
import PageContainer from '@/components/PageContainer.astro';
import Layout from '@/layouts/Layout.astro';
import { TechStackLogoLoop } from '@/components/TechStackLogoLoop';
import { Button } from '@/components/ui/button';
import LightRays from '@/components/LightRays';
---

<Layout>
  <div class='flex h-full flex-col items-center overflow-y-auto scroll-smooth sm:justify-center'>
    <div class='w-full px-4 pt-8 pb-8 sm:px-8 lg:max-w-5xl 2xl:max-w-6xl'>
      <div class='flex w-full flex-col gap-12'>
        <div class='flex flex-col gap-6'>
          <h1 class='text-4xl font-bold md:text-5xl'>Sobre mi</h1>
          <p>
            Estoy en mi último año de Ingeniería en Sistemas y tengo más de 3 años de experiencia
            trabajando como freelance. Desarrollé sitios web, dashboards y soluciones a medida para
            distintos clientes, aprendiendo a gestionar proyectos, comunicarme bien y resolver
            problemas de forma autónoma. Me interesa seguir creciendo dentro de un equipo
            profesional donde pueda aportar, aprender buenas prácticas y trabajar en productos que
            generen impacto real. Soy una persona comprometida, curiosa y con muchas ganas de seguir
            mejorando.
          </p>
          <div class='flex justify-start'>
            <Button>
              <a href='/projects'> Ver mis proyectos </a>
            </Button>
          </div>
        </div>

        <div class='flex w-full flex-col gap-8'>
          <h2 class='text-2xl font-semibold'>Tecnologías con las que he trabajado</h2>
          <TechStackLogoLoop client:load />
        </div>
      </div>
    </div>
  </div>
  <!-- <div class='min-h-screen w-full'>
    <div
      class='absolute inset-0 -z-10 opacity-30'
      style={{
        backgroundImage: `
          linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px),
          linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px)
        `,
        backgroundSize: '60px 60px',
      }}
    >
    </div>
    {/* Your Content/Components */}
  </div> -->

  <div class='absolute inset-0 -z-10 h-full w-full bg-black'>
    <LightRays
      client:load
      raysOrigin='top-center'
      raysColor='#0077c6'
      raysSpeed={0.8}
      fadeDistance={1.2}
      lightSpread={1.5}
      rayLength={20}
      followMouse={false}
      mouseInfluence={0.1}
      noiseAmount={0.1}
      distortion={0.05}
      className='custom-rays'
    />
  </div>
</Layout>
